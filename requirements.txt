# Core dependencies for RGB-to-Skeleton MediaPipe Pipeline
#
# PENTING: mediapipe harus <= 0.10.14
# Mulai versi 0.10.18, mp.solutions.holistic dihapus dari MediaPipe.
# Pipeline ini menggunakan mp.solutions.holistic (legacy API).

# Computer Vision & MediaPipe
mediapipe==0.10.14
opencv-python>=4.8.0

# Numerical
numpy>=1.24.0,<2.0.0

# Optional: visualization extras
matplotlib>=3.7.0
tqdm>=4.65.0

# Web / UI
fastapi>=0.95.0
uvicorn[standard]>=0.22.0
streamlit>=1.22.0
requests>=2.30.0

# GIF generation
Pillow>=9.5.0
